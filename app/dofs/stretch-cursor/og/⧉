input
	vTo
	vFrom

data
	stretch
	cursor

frame
	mag 		= (vTo - vFrom).mag
	stretch = max(mag - deadzone, 0)

	dir 		= dir(vTo, vFrom)
	cursor  = vTo + dir * stretch * strength

design
	deadzone = 0.1
	strength = 3